# Proveedor LLM compatible con OpenAI. Sin esto, el motor corre offline
# con un fallback determinista (no necesitás key para probarlo).

# Cerebras (recomendado por velocidad/costo):
LLM_API_KEY=
LLM_BASE_URL=https://api.cerebras.ai/v1
LLM_MODEL=llama-3.3-70b

# Presupuesto de tokens por llamada. Default 4000. Los modelos de razonamiento
# (p.ej. gpt-oss-120b) gastan tokens "pensando" antes de escribir el JSON, así
# que necesitan espacio de sobra o el content vuelve vacío (finish_reason length).
LLM_MAX_TOKENS=4000

# También sirve cualquier otro compatible con OpenAI (OpenAI, OpenRouter, etc.):
# LLM_BASE_URL=https://openrouter.ai/api/v1
# LLM_MODEL=meta-llama/llama-3.3-70b-instruct
